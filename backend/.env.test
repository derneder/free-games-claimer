# Backend Test Environment Variables
NODE_ENV=test
PORT=3001
HOST=localhost

# Database (SQLite for tests)
DATABASE_URL=postgresql://test:test@localhost:5433/free_games_test

# Redis
REDIS_URL=redis://localhost:6380

# JWT
JWT_SECRET=test-secret-key
JWT_EXPIRATION=1h

# Email
SMTP_HOST=smtp.test.com
SMTP_PORT=587
SMTP_USER=test@test.com
SMTP_PASS=test-password

# CORS
CORS_ORIGIN=http://localhost:5174

# Logging
LOG_LEVEL=error
LOG_FORMAT=json
