sonar.projectKey=derneder
sonar.organization=Derneder_Ram

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=free-games-claimer
sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=backend/src,frontend/src
sonar.tests=backend/tests,frontend/src

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

# Exclusions
sonar.exclusions=**/node_modules/**,**/dist/**,**/coverage/**,**/build/**

# Coverage exclusions
sonar.coverage.exclusions=**/*.test.js,**/*.spec.js,**/*.test.ts,**/*.spec.ts,**/tests/**

# JavaScript/TypeScript specific settings
sonar.javascript.lcov.reportPaths=backend/coverage/lcov.info,frontend/coverage/lcov.info
